import Endpoint from '@/components/Endpoint'

<Endpoint
title="Service information"
method="GET"
category="Generic - Public"
baseUrl="https://stage.server.url"
path="/api/v1/generic/info"
summary="Service information"
description={`Public endpoint for service metadata.`}
hasAuthentication={false}
curl={`curl --request GET \
  --url "{baseUrl}/api/v1/generic/info"`}
languages={{
javascript: `fetch("{baseUrl}/api/v1/generic/info", {
  "method": "GET"
})
  .then(response => response.json())
  .then(data => console.log(data));`,
python: `import requests

url = "{baseUrl}/api/v1/generic/info"

response = requests.get(url)

print(response.json())`
}}
parameters={{
  "cookie": [],
  "header": [],
  "path": [],
  "query": []
}}
requestBody={{
  "properties": [],
  "required": false
}}
responses={{
  "200": []
}}
/>
