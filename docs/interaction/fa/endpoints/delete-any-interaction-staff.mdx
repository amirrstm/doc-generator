<Endpoint
title="Delete any interaction (staff)"
method="DELETE"
category="Staff - Interactions"
baseUrl="https://stage.server.url"
path="/api/v1/staff/interactions/\{interaction_id\}"
summary="Delete any interaction (staff)"
description={`Permanently delete any interaction across all realms. Staff-only endpoint.`}
hasAuthentication={false}
curl={`curl --request DELETE \
  --url "{baseUrl}/api/v1/staff/interactions/123"`}
languages={{
javascript: `fetch("{baseUrl}/api/v1/staff/interactions/\${interaction_id}?realm=value&reason=value", {
  "method": "DELETE"
})
  .then(response => response.json())
  .then(data => console.log(data));`,
python: `import requests

url = "{baseUrl}/api/v1/staff/interactions/{interaction_id}"

params = {
"realm": "value",
"reason": "value"
}

response = requests.delete(url, params=params)

print(response.json())`
}}
parameters={{
  "cookie": [],
  "header": [],
  "path": [
    {
      "description": "No description provided",
      "required": true,
      "title": "interaction_id",
      "type": "string"
    }
  ],
  "query": [
    {
      "description": "Realm ID of the interaction",
      "required": false,
      "title": "realm",
      "type": "unknown"
    },
    {
      "description": "Reason for deletion",
      "required": false,
      "title": "reason",
      "type": "unknown"
    }
  ]
}}
requestBody={{
  "properties": [],
  "required": false
}}
responses={{
  "200": [
    {
      "description": "ID of deleted interaction",
      "title": "interaction_id",
      "type": "string"
    },
    {
      "description": "No description provided",
      "title": "message",
      "type": "string"
    }
  ],
  "422": [
    {
      "description": "No description provided",
      "title": "detail",
      "type": "array",
      "children": {
        "items": [
          {
            "description": "No description provided",
            "title": "loc",
            "type": "array"
          },
          {
            "description": "No description provided",
            "title": "msg",
            "type": "string"
          },
          {
            "description": "No description provided",
            "title": "type",
            "type": "string"
          }
        ],
        "title": "ValidationError"
      }
    }
  ]
}}
/>
