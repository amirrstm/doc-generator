<Endpoint
title="Health Check"
method="GET"
category="General"
baseUrl="https://stage.server.url"
path="/health"
summary="Health Check"
description={`Health check endpoint`}
hasAuthentication={false}
curl={`curl --request GET \
  --url "{baseUrl}/health"`}
languages={{
javascript: `fetch("{baseUrl}/health", {
  "method": "GET"
})
  .then(response => response.json())
  .then(data => console.log(data));`,
python: `import requests

url = "{baseUrl}/health"

response = requests.get(url)

print(response.json())`
}}
parameters={{
  "cookie": [],
  "header": [],
  "path": [],
  "query": []
}}
requestBody={{
  "properties": [],
  "required": false
}}
responses={{
  "200": []
}}
/>
