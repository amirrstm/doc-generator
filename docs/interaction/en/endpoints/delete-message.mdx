import Endpoint from '@/components/Endpoint'

<Endpoint
title="Delete message"
method="DELETE"
category="Enduser - Messages"
baseUrl="https://stage.server.url"
path="/api/v1/enduser/\{message_id\}"
summary="Delete message"
description={`Soft delete a message (only if user owns it).`}
hasAuthentication={false}
curl={`curl --request DELETE \
  --url "{baseUrl}/api/v1/enduser/123"`}
languages={{
javascript: `fetch("{baseUrl}/api/v1/enduser/\${message_id}", {
  "method": "DELETE"
})
  .then(response => response.json())
  .then(data => console.log(data));`,
python: `import requests

url = "{baseUrl}/api/v1/enduser/{message_id}"

response = requests.delete(url)

print(response.json())`
}}
parameters={{
  "cookie": [],
  "header": [],
  "path": [
    {
      "description": "No description provided",
      "required": true,
      "title": "message_id",
      "type": "string"
    }
  ],
  "query": []
}}
requestBody={{
  "properties": [],
  "required": false
}}
responses={{
  "422": [
    {
      "description": "No description provided",
      "title": "detail",
      "type": "array",
      "children": {
        "items": [
          {
            "description": "No description provided",
            "title": "loc",
            "type": "array"
          },
          {
            "description": "No description provided",
            "title": "msg",
            "type": "string"
          },
          {
            "description": "No description provided",
            "title": "type",
            "type": "string"
          }
        ],
        "title": "ValidationError"
      }
    }
  ]
}}
/>
