<Endpoint
title="Impersonate User"
method="POST"
category="Onboarding"
baseUrl="https://api.paystar.stg.felesh.ai"
path="/auth/api/v1/staff/realms/\{slug\}/impersonate"
summary="Impersonate User"
description={``}
  hasAuthentication={false}
  curl={`curl --request POST \
 --url "{baseUrl}/auth/api/v1/staff/realms/example-value/impersonate" \
 --header "Content-Type: application/json" \
 --header "X-Client-Id: value" \
 --header "X-Client-Secret: value" \
 --data '{
"username": "string"
}'`}
  languages={{
    javascript: `const requestBody = {
"username": "string"
};

fetch("{baseUrl}/auth/api/v1/staff/realms/\${slug}/impersonate", {
"method": "POST",
"headers": {
"Content-Type": "application/json"
},
"body": JSON.stringify(requestBody)
})
.then(response => response.json())
.then(data => console.log(data));`,
    python: `import requests

url = "{baseUrl}/auth/api/v1/staff/realms/{slug}/impersonate"

payload = {
"username": "string"
}

headers = {
"Content-Type": "application/json",
"X-Client-Id": "<token>",
"X-Client-Secret": "<token>"
}

response = requests.post(url, json=payload, headers=headers)

print(response.json())`
}}
parameters={{
  "cookie": [],
  "header": [
    {
      "description": "No description provided",
      "required": false,
      "title": "X-Client-Id",
      "type": "string"
    },
    {
      "description": "No description provided",
      "required": false,
      "title": "X-Client-Secret",
      "type": "string"
    }
  ],
  "path": [
    {
      "description": "The realm slug (Paystar customer's user_id) returned from the onboard response.",
      "required": true,
      "title": "slug",
      "type": "string"
    }
  ],
  "query": []
}}
requestBody={{
  "contentType": "application/json",
  "properties": [
    {
      "description": "The customer's username (mobile number or email address).",
      "title": "username",
      "type": "string",
      "required": true
    }
  ],
  "required": true
}}
responses={{
  "200": [
    {
      "description": "JWT access token. Use as Bearer token for all subsequent API calls.",
      "title": "access_token",
      "type": "string"
    },
    {
      "description": "Access token expiry time in seconds.",
      "title": "expires_in",
      "type": "integer"
    },
    {
      "description": "Refresh token expiry time in seconds.",
      "title": "refresh_expires_in",
      "type": "integer"
    },
    {
      "description": "JWT refresh token for obtaining new access tokens.",
      "title": "refresh_token",
      "type": "string"
    },
    {
      "description": "Token type (always \"Bearer\").",
      "title": "token_type",
      "type": "string"
    },
    {
      "description": "Session identifier.",
      "title": "session_state",
      "type": "string"
    },
    {
      "description": "Granted OAuth scopes.",
      "title": "scope",
      "type": "string"
    }
  ],
  "422": [
    {
      "description": "No description provided",
      "title": "detail",
      "type": "array",
      "children": {
        "items": [
          {
            "description": "No description provided",
            "title": "loc",
            "type": "array"
          },
          {
            "description": "No description provided",
            "title": "msg",
            "type": "string"
          },
          {
            "description": "No description provided",
            "title": "type",
            "type": "string"
          },
          {
            "description": "No description provided",
            "title": "input",
            "type": "unknown"
          },
          {
            "description": "No description provided",
            "title": "ctx",
            "type": "object"
          }
        ],
        "title": "Auth_ValidationError"
      }
    }
  ]
}}
/>
